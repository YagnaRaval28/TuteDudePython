Task 1: Read File with Exception Handling
1 Opens the file 'Sample.txt' in read text mode ('rt').
2 Reads and prints the first two lines from the file.
3 Uses try-except block to handle FileNotFoundError.
4 Displays an appropriate message if the file is missing.


Task 2: Write, Append, and Display File Content
1 Takes user input and writes it to 'output.txt' using write mode ('wt').
2 Takes additional input and appends it to the same file using append mode ('at').
3 Reads the file again to display its final content.
4 Demonstrates file handling using write, append, and read operations in Python.